defaults.org=foxford-dc
defaults.project=node
# Sentry CLI will automatically pick up SENTRY_AUTH_TOKEN from the environment.
# Ensure SENTRY_AUTH_TOKEN is set in your CI/CD environment.
#
# For local development, you can uncomment and set the token here,
# or log in using `npx sentry-cli login`
# auth.token=YOUR_AUTH_TOKEN
